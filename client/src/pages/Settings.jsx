import { motion } from 'framer-motion';

const Settings = () => {
  return (
    <div>
      <h1>$ settings</h1>
      <p>You can add theme options, font size, etc. here.</p>
    </div>
  );
};

export default Settings;
